{
  "name": "lamdbapgrds",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "build": "rm -rf index.cjs dist.zip && esbuild index.mjs --bundle --platform=node --target=esnext --outfile=index.cjs && zip dist.zip ./index.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1534.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}
